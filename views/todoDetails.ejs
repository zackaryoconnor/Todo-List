<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= todo.item %>
    </title>
    <link rel="stylesheet" href="/css/styles.css">
</head>

<body>

    <div class="container-view">
        <div class="todos-container" id="todos-details-container">
            <a href="/">Cancel</a>
            <h1>Edit <%= todo.item %>
            </h1>

            <form action="/todo/<%= todo._id %>?_method=PUT" method="POST">
                <input type="text" name="item" value="<%= todo.item %>">
                <button type="submit" id="update-button">Update Item</button>
            </form>

            <form action="/todo/<%= todo._id %>?_method=DELETE" method="POST">
                <button type="submit" id="delete-button">Delete <%= todo.item %></button>
            </form>
        </div>
    </div>

</body>

</html>